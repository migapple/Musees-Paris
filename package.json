{
  "name": "musees-paris",
  "version": "1.0.0",
  "description": "Musées de Paris — Application iOS Capacitor",
  "main": "index.js",
  "scripts": {
    "start": "npx serve www -l 3000",
    "build": "echo 'No build step needed — static www/'",
    "sync": "npx cap sync ios",
    "open": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "serve": "^14.2.0"
  }
}